* Proper logging...
* Exception wrapping in calls from Connector(?)
* Check encoding in file I/O from stdin/out
* pip install / setup
* get rid of old stuff

* procs take config params as kwargs

* cmdline keyboard interrupt chain... what happens(?)... make first break stop, second abort

* Make README.md **REAL** markdown
  - inline code with backticks
  - bold and italics



r = ElasticReader(name="myesreader", index="bla", doctype="bla")
    self.config.name = "myesreader"
    self.config.index = "bla"
    self.config.doctype="bla"

    self.name=config.name


r = EReader("myreader")

r = EReader()

r = EReader(name="hei", index="blei")

config
  .name
  .index

config = {name=...}

r = EReader(**config)
